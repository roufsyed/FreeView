<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Freediumcfd" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Soft off-white surfaces for a gentler light mode (light theme only). -->
        <item name="android:colorBackground">@color/off_white</item>
        <item name="colorSurface">@color/off_white</item>
        <!-- Required by PreferenceFragmentCompat in SettingsActivity. -->
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <style name="Theme.Freediumcfd" parent="Base.Theme.Freediumcfd" />

    <style name="Theme.Freediumcfd.Splash" parent="Theme.Freediumcfd">
        <item name="android:windowBackground">@color/brand_blue</item>
        <item name="android:windowSplashScreenBackground" tools:targetApi="31">@color/brand_blue</item>
        <item name="android:windowSplashScreenIconBackgroundColor" tools:targetApi="31">@color/brand_blue</item>
    </style>
</resources>