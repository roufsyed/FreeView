<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Freediumcfd">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Freediumcfd.Splash"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!--
              Deep-link: offer FreeView when ANY web link (http/https) is tapped, so Medium articles
              on medium.com AND on custom publication domains (ai.plainenglish.io, towardsdatascience.com,
              …) all open here - routed through the reader service exactly like a pasted link. No host
              restriction (custom domains are unbounded); the scheme is gated in code (isAcceptableViewUri)
              to http/https only, and MediumService.buildUrl is the execution boundary. Intentionally NO
              android:autoVerify (we don't control these domains). Trade-off: this makes FreeView a
              browser-type handler offered for all web links - pick it per-link from the chooser rather
              than setting it as the default browser. Kept as its own filter so MAIN/LAUNCHER and SEND stay intact.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:label="@string/history_title"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".BookmarksActivity"
            android:exported="false"
            android:label="@string/bookmarks_title"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".FaqActivity"
            android:exported="false"
            android:label="@string/faq_title"
            android:parentActivityName=".SettingsActivity" />
    </application>

</manifest>